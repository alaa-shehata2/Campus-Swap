{
  "name": "campus-swap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CampusSwap MVP — TypeScript modular monolith (Phase 1: identity + listings + discovery)",
  "scripts": {
    "test": "tsx --test \"tests/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
